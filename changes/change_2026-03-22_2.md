# change_2026-03-22_2

## Действия
- `QlikSyntaxHighlighter`: для `QLIK_FUNCTION` дефолтный цвет заменён с `FUNCTION_CALL` на `KEYWORD` — токены из `functions.txt` не наследовали видимый цвет в схеме IDE.

## Сборка
- `docker compose run --rm qlik-idea-plugin-build`

## Следующие шаги
- Переустановить плагин; при желании развести цвета: Editor → Color Scheme → Qlik (если добавят страницу настроек) или сменить дефолт у `QLIK_FUNCTION` на `INSTANCE_METHOD`.
