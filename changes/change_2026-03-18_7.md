# change_2026-03-18_7

Сделано:
- Исправлена сборка Gradle: вместо `pycharmPC` используем базу `IC` (IntelliJ IDEA Community) той же платформы 2023.3.

Причина:
- `gradle-intellij-plugin` падает на скачанном `pycharmPC-2023.3` из-за некорректного `<xi:include>` в одном из bundled plugins (`pycharm-community-customization`).

Следующие шаги:
- Перезапустить сборку: `docker compose run --rm qlik-idea-plugin-build`.

