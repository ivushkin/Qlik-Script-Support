# change_2026-03-22_1

## Действия
- Расширен `src/main/resources/qlik/keywords.txt`: SQL и смежные токены (ORDER, BY, LIMIT, GROUP, HAVING, TABLE, TAG, IS, NULL, CAST, UNION, INTERSECT, INTO, PARTITION, WINDOW, FIELDVALUE, FIELDVALUECOUNT, DATE и др.) для корректной классификации лексером как KEYWORD.

## Сборка
- Docker Desktop недоступен (pipe к engine не найден); локально: `docker compose run --rm qlik-idea-plugin-build` при запущенном Docker.

## Следующие шаги
- Пересобрать/установить плагин в IDE и проверить подсветку на вашем скрипте.
- При необходимости добавить редкие SQL/Postgres токены (`RETURNING`, `LATERAL`, …) в `keywords.txt`.
