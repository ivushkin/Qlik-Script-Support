# change_2026-03-18_2

Сделано:
- Подтянуты списки функций/ключевых слов из грамматики VS Code плагина ([bintocher/vscode-qlik-editor](https://github.com/bintocher/vscode-qlik-editor)).
- Добавлены ресурсы: `src/main/resources/qlik/keywords.txt`, `src/main/resources/qlik/functions.txt`.
- `QlikLexer` переведен на загрузку `KEYWORDS`/`FUNCTIONS` из ресурсов (без хардкода в Kotlin).
- Добавлен генератор списков `tools/extract_lists.ps1` (Windows).

Числа:
- keywords: 106
- functions: 427

Следующие шаги:
- Дожать парсер ключевых слов/операторов (добавить multi-word конструкции в виде отдельных слов там, где нужно).
- Если нужен 100% список функций (433), расширить extractor, чтобы не терялись элементы с символами `#`/`_` и спец-именами (например `Timestamp#`).
- Добавить отдельный токен/подсветку для макросов `$(...)`.

