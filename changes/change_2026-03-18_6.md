# change_2026-03-18_6

Сделано:
- Починен docker-сценарий сборки: сборка Gradle больше не запускается на этапе `docker build`.
- `docker-compose.yml` переведен на `image: gradle:8.7-jdk17`, добавлен volume для кеша Gradle.
- Для диагностики добавлен `--stacktrace` в команду сборки.

Следующие шаги:
- Запустить `docker compose run --rm qlik-idea-plugin-build` и при ошибке прислать вывод Gradle после строки `> Task ... FAILED`.

