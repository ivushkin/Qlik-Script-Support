# change_2026-03-18_3

Сделано:
- Починен extractor `tools/extract_lists.ps1` для функций вида `Name\#` из грамматики VS Code.
- Перегенерированы ресурсы `src/main/resources/qlik/functions.txt`.

Числа:
- keywords: 106
- functions: 433 (включая `DATE#`, `INTERVAL#`, `MONEY#`, `NUM#`, `TIME#`, `TIMESTAMP#`)

Следующие шаги:
- При желании добавить подсветку макросов `$(...)` отдельным токеном.

