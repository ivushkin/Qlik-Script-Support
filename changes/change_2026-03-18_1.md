# change_2026-03-18_1

Сделано:
- Добавлен минимальный проект IntelliJ-плагина (Gradle + Kotlin) под PyCharm 2023.3.
- Зарегистрирован язык `QlikScript`, file type для расширений `qvs;qlik`, и factory подсветки.
- Реализован простой lexer (без парсера) для: ключевых слов, функций, строк, чисел, комментариев, операторов.
- Добавлена docker-сборка (`Dockerfile`, `docker-compose.yml`).

Файлы:
- `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties`
- `src/main/resources/META-INF/plugin.xml`
- `src/main/kotlin/local/qlik/lang/*`
- `Dockerfile`, `docker-compose.yml`, `README.md`

Следующие шаги:
- Расширить списки `KEYWORDS`/`FUNCTIONS` (можно импортировать полный список функций из спецификации/BNF).
- Добавить `ColorSettingsPage`, чтобы настраивать цвета в UI PyCharm.
- (Опционально) Добавить PSI/Parser для навигации/структуры и автодополнения.

